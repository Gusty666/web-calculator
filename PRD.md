# 🚀 高级科学计算器 - 产品需求文档 (PRD v2.0)

**项目名称**: 高级科学计算器  
**当前版本**: 2.0  
**最后更新**: 2026年4月24日  
**维护者**: Gusty666

---

## 📋 目录

1. [项目概述](#项目概述)
2. [核心功能需求](#核心功能需求)
3. [交互与视觉需求](#交互与视觉需求)
4. [逻辑处理与异常处理](#逻辑处理与异常处理)
5. [非功能性需求](#非功能性需求)
6. [实现状态](#实现状态)

---

## 项目概述

高级科学计算器是一个web应用，旨在提供功能强大、用户体验良好的计算工具。项目采用**原生HTML/CSS/JavaScript**开发，无第三方框架依赖，支持复杂科学运算和移动端自适应。

**核心理念**: `"思路明确" -> 用户体验优先 -> 鲁棒错误处理 -> 代码质量第一"`

---

## 核心功能需求

### 1.1 基础运算（Functional Requirement #1）

- **加法 (+)**: 连续加法运算，如 `5 + 3 + 2 = 10`
- **减法 (-)**: 连续减法运算，如 `10 - 2 - 3 = 5`
- **乘法 (*)**: 连续乘法运算，如 `2 * 3 * 4 = 24`
- **除法 (/)**: 连续除法运算，如 `24 / 2 / 3 = 4`

**优先级**: 遵循标准数学优先级（`乘除优于加减`），确保 `1 + 2 * 3 = 7` 而非 `9`。

**实现方式**: 使用原生 `eval()` 函数处理运算符优先级。

| 测试用例 | 输入 | 期望输出 |
|---------|------|---------|
| 加法 | `5 + 3` | `8` |
| 优先级 | `1 + 2 * 3` | `7` |
| 链式运算 | `10 - 2 - 3` | `5` |

---

### 1.2 高级运算（Functional Requirement #2）

#### 1.2.1 根号 (√)

**功能描述**: 对当前输入值计算平方根。

- **有效输入**: 非负数 (>= 0)
  - `√9 = 3`
  - `√0 = 0`
  - `√2 ≈ 1.414`

- **异常处理**:
  - 负数输入 → 显示 "非法输入" (数学上复数域不适合基础计算器)
  - 空输入 → 默认为 0，结果为 0

**实现**: [script.js - applyMathFunction('sqrt')](script.js)

| 测试用例 | 输入 | 期望输出 |
|---------|------|---------|
| 完全平方 | `√16` | `4` |
| 无理数 | `√2` | `1.414...` |
| 零 | `√0` | `0` |
| 负数异常 | `√-1` | `非法输入` |

---

#### 1.2.2 阶乘 (x!)

**功能描述**: 对当前输入值计算阶乘。定义: `n! = n × (n-1) × ... × 1`, `0! = 1`

- **有效输入**: 非负整数
  - `5! = 120`
  - `0! = 1`
  - `10! = 3628800`

- **异常处理**:
  - **非整数输入** → 显示 "非整数或负数"
    - `3.5! → 非整数或负数`
    - `4.0!` → 允许 (强制转换为整数)
  - **负数输入** → 显示 "非整数或负数"
    - `-5! → 非整数或负数`
  - **溢出异常** → 显示 "数值溢出"
    - `171! 以上` → Infinity (超出JavaScript精度范围)

**实现**: [script.js - applyMathFunction('factorial')](script.js)

| 测试用例 | 输入 | 期望输出 |
|---------|------|---------|
| 零阶乘 | `0!` | `1` |
| 正整数 | `5!` | `120` |
| 非整数异常 | `3.5!` | `非整数或负数` |
| 负数异常 | `-5!` | `非整数或负数` |
| 溢出 | `171!` | `数值溢出` |

---

#### 1.2.3 幂运算

**平方 (x²)**:
- `2² = 4`
- `(-3)² = 9`
- 支持小数: `2.5² = 6.25`

**立方 (x³)**:
- `2³ = 8`
- `(-2)³ = -8`
- 支持小数: `2.5³ = 15.625`

**实现**: [script.js - applyMathFunction('power'/'power3')](script.js)

| 测试用例 | 输入 | 期望输出 |
|---------|------|---------|
| 正数平方 | `3²` | `9` |
| 负数平方 | `(-2)²` | `4` |
| 立方 | `2³` | `8` |
| 小数 | `2.5²` | `6.25` |

---

### 1.3 输入控制（Functional Requirement #3）

#### 1.3.1 DEL（删除）

**功能**: 退格，删除输入的最后一个字符。

- `12345` → DEL → `1234`
- `3.14` → DEL → `3.1`

**实现**: 使用 `String.slice(0, -1)` 移除最后一个字符。

---

#### 1.3.2 C（清除）

**功能**: 清空显示屏和所有计算状态。

- 清空显示屏内容
- 重置 `justCalculated` 标志

---

#### 1.3.3 小数点 (.)

**功能**: 在当前数字中插入小数点。

**关键逻辑 - 防重复输入**:
- 同一数字内只能出现 **一个小数点**
  - `1.2.3` ❌ (第三个 `.` 被拒绝)
  - `1.2 + 3.4` ✅ (两个数字各有一个)

**实现细节**:
```javascript
// 在 appendToDisplay() 中检查当前数字是否已有小数点
if (input === '.') {
    const lastNumber = display.value.split(/[\+\-\*\/]/).pop();
    if (lastNumber.includes('.')) {
        return; // 已有小数点，不再输入
    }
}
```

| 测试用例 | 输入 | 显示结果 |
|---------|------|---------|
| 首个小数点 | `3.` | `3.` |
| 重复小数点拒绝 | `3..` | `3.` (第二个被拒) |
| 不同数字小数点 | `3. + 4.` | `3. + 4.` ✅ |

---

## 交互与视觉需求

### 2.1 响应式布局 (RWD)

#### PC端 (>1024px)

- **布局**: 计算器固定宽度 **360px**，居中显示
- **显示屏高度**: 80px
- **按钮高度**: 65px
- **间距**: 12px

#### 平板端 (768px - 1024px)

- **计算器宽度**: 90%，最大500px
- **显示屏高度**: 70px
- **按钮高度**: 55px
- **间距**: 10px

#### 手机端 (480px - 768px)

- **计算器宽度**: 95%
- **显示屏高度**: 60px
- **按钮高度**: 50px
- **间距**: 8px
- **允许垂直滚动**: `overflow-y: auto`

#### 超小屏幕 (<360px)

- **计算器宽度**: 98%
- **显示屏高度**: 55px
- **按钮高度**: 45px
- **间距**: 6px

**实现**: [style.css - @media queries](style.css#L109-L180)

---

### 2.2 视觉风格

#### Glassmorphism（磨砂玻璃）

- **计算器背景**: `rgba(255, 255, 255, 0.2)` + `backdrop-filter: blur(15px)`
- **边框**: `rgba(255, 255, 255, 0.3)` 半透明边框
- **阴影**: `0 25px 45px rgba(0,0,0,0.1)` (柔和投影)

#### 动态背景

- **主球** (左上): 紫色渐变 `#667eea → #764ba2`，尺寸 400px，blur 80px
- **副球** (右下): 红色渐变 `#ff9a9e`，尺寸 400px，blur 80px
- **不透明度**: 0.4 (保持页面可见性)

**目的**: 增强视觉层次感，避免单调感。

---

### 2.3 反馈机制

#### 按钮点击反馈

**Hover状态**:
```css
.btn:hover {
    background: rgba(255, 255, 255, 0.8);  /* 更透明 */
    transform: translateY(-2px);             /* 向上微动 */
}
```

**Active状态** (按下时):
```css
.btn:active {
    transform: translateY(1px);              /* 向下微动，模拟下压效果 */
}
```

#### 颜色分区

- **运算符按钮** (+, -, *, /, C, DEL): 橙色 `#ff9f43`
- **科学键** (√, x!, x², x³): 蓝色 `#0abde3`
- **等号按钮** (=): 紫色渐变 (突出重要性)
- **普通数字**: 白色半透明

---

### 2.4 状态显示

#### 错误提示

| 错误类型 | 显示信息 | 原因 |
|---------|---------|------|
| 除以零 | `不能除以零` | 用户输入 `1/0=` |
| 非法根号 | `非法输入` | 负数开根号 |
| 非整数阶乘 | `非整数或负数` | 输入 `3.5!` 或 `-5!` |
| 数值溢出 | `数值溢出` | 阶乘超出范围 |
| 语法错误 | `语法错误` | 表达式不完整，如 `5 +` 直接按 `=` |
| 通用计算错误 | `计算错误` | 其他异常 |

#### 超大数值处理

当计算结果超过 **±10^15** 时，自动转换为科学计数法:
- `9999999999999999 + 1` → `1.000000e+16`
- `0.000000000000001` → `1.000000e-15`

**实现**: `result.toExponential(6)` (6位有效数字)

---

## 逻辑处理与异常处理

### 3.1 运算优先级（Operator Precedence）

采用标准数学优先级:

| 优先级 | 运算符 | 示例 |
|--------|--------|------|
| 1 (最高) | `√`, `x!`, `x²`, `x³` | `√16 = 4` |
| 2 | `*`, `/` | `2 + 3 * 4 = 14` |
| 3 (最低) | `+`, `-` | `1 + 2 + 3 = 6` |

**实现**: JavaScript 原生 `eval()` 自动遵循此优先级。

---

### 3.2 数学异常处理

#### 3.2.1 除以零 (Division by Zero)

```javascript
if (!isFinite(result)) {
    display.value = "不能除以零";
}
```

- `5 / 0` → "不能除以零"
- `0 / 0` → "不能除以零"

#### 3.2.2 负数根号 (Negative Square Root)

```javascript
if (value < 0) {
    display.value = "非法输入";
    return;
}
```

- `√-4` → "非法输入"
- `√-0.5` → "非法输入"

#### 3.2.3 非整数阶乘 (Non-Integer Factorial)

```javascript
if (!Number.isInteger(value) || value < 0) {
    display.value = "非整数或负数";
    return;
}
```

- `3.5!` → "非整数或负数"
- `-5!` → "非整数或负数"
- `4.0!` → `24` (自动转换为整数)

#### 3.2.4 数值溢出 (Overflow)

```javascript
if (!isFinite(result)) {
    display.value = "数值溢出";
    return;
}
```

- `171!` 及以上 → "数值溢出"

---

### 3.3 状态管理

**`justCalculated` 标志**:

| 状态 | 含义 | 行为 |
|------|------|------|
| `true` | 刚完成计算 | 用户继续输入数字时，清空屏幕后输入；输入运算符时，继续追加 |
| `false` | 用户正在输入 | 追加到现有内容 |

**流程**:
1. 用户输入 `5` + `3` + `=` → 显示 `8`, `justCalculated = true`
2. 用户按 `2` → 清空屏幕，显示 `2`, `justCalculated = false`
3. 用户按 `+` → 追加 `+`, 显示 `2+`, `justCalculated = false`

---

## 非功能性需求

### 4.1 性能 (Performance)

- **计算响应延迟**: < **100ms**
  - 通过原生JavaScript实现，无外部库延迟
  - 简单的数学运算 (<10ms)
  - 阶乘等复杂运算 (<50ms)

- **页面加载时间**: < **1s**
  - HTML: ~300 bytes
  - CSS: ~3KB
  - JS: ~8KB
  - 无异步资源

---

### 4.2 兼容性 (Browser Compatibility)

| 浏览器 | PC | 移动 | 备注 |
|--------|-----|------|------|
| Chrome | ✅ 90+ | ✅ 90+ | 推荐环境 |
| Firefox | ✅ 88+ | ✅ 88+ | 完全支持 |
| Safari | ✅ 14+ | ✅ 14+ | iOS 14+ |
| Edge | ✅ 90+ | ✅ 90+ | 基于Chromium |

**关键API支持**:
- `eval()` - 所有浏览器原生支持
- `Math.*` - 所有浏览器原生支持
- `backdrop-filter` - 需要较新浏览器 (兼容性95%+)
- `CSS Grid` - 所有现代浏览器支持

---

### 4.3 部署 (Deployment)

#### GitHub Pages 自动部署

- **触发条件**: Push to `main` 分支
- **部署流程**: GitHub Actions 工作流
- **输出目录**: `/` (项目根目录)
- **访问URL**: `https://Gusty666.github.io/web-calculator`

#### 部署文件

```
web-calculator/
├── index.html        (entry point)
├── style.css         (styles)
├── script.js         (logic)
├── PRD.md           (this document)
├── README.md        (user guide)
└── LICENSE          (MIT)
```

---

### 4.4 代码质量 (Code Quality)

#### 4.4.1 代码结构

- **HTML/CSS/JS 完全分离**
  - HTML 仅包含语义结构
  - CSS 独立样式文件
  - JavaScript 独立逻辑文件

#### 4.4.2 命名规范

**JavaScript变量/函数**:
- 驼峰命名法: `justCalculated`, `calculateResult()`
- 布尔值前缀: `is`, `has` → `isOperator()`

**CSS类名**:
- 连字符命名法: `.btn-operator`, `.display-container`
- 意义明确: `.btn-equal`, `.scientific-key`

#### 4.4.3 注释与文档

**JavaScript注释**:
- 函数必须有 JSDoc 注释
- 复杂逻辑需加行注释
- 中英文混合允许

示例:
```javascript
/**
 * 通用数学函数处理器
 * @param {string} funcType - 函数类型：'sqrt'|'factorial'|'power'|'power3'
 */
function applyMathFunction(funcType) {
    // ...
}
```

**CSS注释**:
- 分区标注: `/* ========== 响应式设计 ========== */`
- 关键规则注释

#### 4.4.4 代码复用 (DRY - Don't Repeat Yourself)

**重构方案**:

❌ **重复代码**:
```javascript
function calculatePower() {
    try { 
        display.value = Math.pow(eval(display.value), 2); 
        justCalculated = true; 
    } catch(e) { display.value = "错误"; }
}
function calculatePower3() {
    try { 
        display.value = Math.pow(eval(display.value), 3); 
        justCalculated = true; 
    } catch(e) { display.value = "错误"; }
}
```

✅ **通用函数**:
```javascript
function applyMathFunction(funcType) {
    try {
        let value = eval(display.value.trim());
        let result;
        
        switch (funcType) {
            case 'power': result = Math.pow(value, 2); break;
            case 'power3': result = Math.pow(value, 3); break;
            // ...
        }
        
        display.value = result;
        justCalculated = true;
    } catch(error) {
        display.value = errorMessage;
    }
}

// 简化的包装函数
function calculatePower() { applyMathFunction('power'); }
function calculatePower3() { applyMathFunction('power3'); }
```

---

## 实现状态

### ✅ 已完成功能

| 功能 | 实现时间 | 状态 | 文件 |
|------|---------|------|------|
| 基础运算 (+,-,*,/) | v1.0 | ✅ 完成 | [script.js](script.js#L46-L68) |
| 根号运算 (√) | v1.0 | ✅ 完成 | [script.js](script.js#L75-L92) |
| 阶乘运算 (x!) | v1.0 | ✅ 完成 | [script.js](script.js#L96-L122) |
| 平方/立方 (x²/x³) | v1.0 | ✅ 完成 | [script.js](script.js#L126-L155) |
| DEL/C 按钮 | v1.0 | ✅ 完成 | [script.js](script.js#L37-L43) |
| 小数点防重复 | v2.0 | ✅ 完成 | [script.js](script.js#L8-L19) |
| 除以零提示 | v2.0 | ✅ 完成 | [script.js](script.js#L46-L68) |
| 负数根号异常 | v2.0 | ✅ 完成 | [script.js](script.js#L108-L112) |
| 非整数阶乘异常 | v2.0 | ✅ 完成 | [script.js](script.js#L123-L127) |
| 超大数值处理 | v2.0 | ✅ 完成 | [script.js](script.js#L57-L59) |
| 通用函数重构 | v2.0 | ✅ 完成 | [script.js](script.js#L72-L155) |
| Glassmorphism 风格 | v1.0 | ✅ 完成 | [style.css](style.css#L33-L40) |
| 响应式布局 | v2.0 | ✅ 完成 | [style.css](style.css#L110-L180) |
| 按钮交互反馈 | v1.0 | ✅ 完成 | [style.css](style.css#L80-L88) |
| 动态背景效果 | v1.0 | ✅ 完成 | [style.css](style.css#L21-L30) |

### 🔄 版本迭代历程

**v1.0 (初版)**
- 基础计算功能
- Glassmorphism设计
- 关键函数实现

**v2.0 (完善版) - 当前**
- ➕ 小数点防重复机制
- ➕ 完善的异常处理体系
- ➕ 代码复用 (通用函数)
- ➕ 响应式设计 (4档布局)
- ➕ 详细PRD文档

---

## � 业务逻辑流程图

### 用户交互状态机

```
┌─────────────┐
│   初始状态   │ (显示屏为空, justCalculated=false)
└──────┬──────┘
       │
       ├─→ [输入数字] ──→ ┌──────────────┐
       │                 │  输入状态      │ (justCalculated=false)
       │                 │  显示: 5       │
       │                 └────┬─────────┘
       │                      │
       │                      ├─→ [继续输入数字] ──→ 显示: 5 → 53
       │                      │
       │                      ├─→ [输入运算符] ──→ ┌──────────────┐
       │                      │                    │  运算符状态   │
       │                      │                    │  显示: 53+    │
       │                      │                    └────┬────────┘
       │                      │                         │
       │                      │                         └─→ [输入数字] ──→ [继续输入数字]
       │                      │
       │                      └─→ [科学函数] ──→ 计算结果 ──→ ┌──────────────┐
       │                                                     │  结果状态     │
       │                                                     │  显示: 7      │
       │                                                     │  jC=true      │
       │                                                     └────┬────────┘
       │                                                          │
       └──────────────────────────────────────────────────────────┘
                    [清除C或点击数字重新开始]

异常处理:
  ├─ 除数为零 → "不能除以零"
  ├─ 负数根号 → "非法输入"
  ├─ 非整数阶乘 → "非整数或负数"
  └─ 大阶乘溢出 → "数值溢出"
```

---

## 🧪 完整测试用例矩阵

### A. 基础运算测试 (10个用例)

| # | 场景 | 输入序列 | 期望输出 | 实际状态 |
|---|------|--------|---------|--------|
| A1 | 简单加法 | `1` + `2` `=` | `3` | ✅ |
| A2 | 运算符优先级 | `2` + `3` × `4` `=` | `14` | ✅ |
| A3 | 连续除法 | `24` ÷ `2` ÷ `3` `=` | `4` | ✅ |
| A4 | 负数计算 | `-5` + `3` `=` | `-2` | ✅ |
| A5 | 小数计算 | `1.5` + `2.5` `=` | `4` | ✅ |
| A6 | 零的运算 | `0` × `99` `=` | `0` | ✅ |
| A7 | 超大数值 | `999999999` × `999999999` `=` | 科学计数法 | ✅ |
| A8 | 单数字 | `5` `=` | `5` | ✅ |
| A9 | 连续点击等号 | `5` + `3` `=` `=` | `8` (第二次不变) | ✅ |
| A10 | 空屏幕按等号 | `=` | 无响应 | ✅ |

### B. 科学函数测试 (12个用例)

| # | 场景 | 输入 | 期望输出 | 实际状态 |
|---|------|-----|---------|--------|
| B1 | 完全平方 | `16` √ | `4` | ✅ |
| B2 | 完全立方根 | `8` (3√) | - | - |
| B3 | 零的根号 | `0` √ | `0` | ✅ |
| B4 | 无理数根号 | `2` √ | `1.414...` | ✅ |
| B5 | **负数根号** | `-4` √ | `非法输入` | ✅ |
| B6 | 零阶乘 | `0` x! | `1` | ✅ |
| B7 | 正整数阶乘 | `5` x! | `120` | ✅ |
| B8 | **非整数阶乘** | `3.5` x! | `非整数或负数` | ✅ |
| B9 | **负数阶乘** | `-5` x! | `非整数或负数` | ✅ |
| B10 | **大数阶乘** | `171` x! | `数值溢出` | ✅ |
| B11 | 平方 | `7` x² | `49` | ✅ |
| B12 | 立方 | `3` x³ | `27` | ✅ |

### C. 输入控制测试 (8个用例)

| # | 场景 | 操作 | 期望结果 | 实际状态 |
|---|------|-----|--------|--------|
| C1 | 正常小数点 | `3` `.` `14` | 显示 `3.14` | ✅ |
| C2 | **小数点防重复** | `3` `.` `.` | 显示 `3.` (第二个被拒) | ✅ |
| C3 | 多个数的小数点 | `3` `.` + `4` `.` `5` | 显示 `3. + 4.5` | ✅ |
| C4 | DEL删除 | `12345` DEL DEL | 显示 `123` | ✅ |
| C5 | C清除 | `5` + `3` C | 显示空 | ✅ |
| C6 | 计算后输入数字 | `5` + `3` `=` → `2` | 清空后显示 `2` | ✅ |
| C7 | 计算后输入运算符 | `5` + `3` `=` → `×` | 显示 `8×` | ✅ |
| C8 | 连续运算符 | `5` + `+` | 显示 `5+` (后者覆盖前者) | ⚠️ 需确认 |

### D. 异常处理测试 (7个用例)

| # | 场景 | 输入 | 期望输出 | 实际状态 |
|---|------|-----|---------|--------|
| D1 | **除以零** | `5` ÷ `0` `=` | `不能除以零` | ✅ |
| D2 | 零除以零 | `0` ÷ `0` `=` | `不能除以零` | ✅ |
| D3 | **语法错误** | `5` + `=` | `语法错误` | ✅ |
| D4 | **非法表达式** | `+ 5` `=` | `计算错误` | ✅ |
| D5 | 超长数字 | 999位数字 | 显示或科学计数法 | ⚠️ 浏览器限制 |
| D6 | 特殊字符输入 | `@#$%` | 忽略(onclick防护) | ✅ |
| D7 | 极小数值 | `0.00001` √ | `0.00316...` | ✅ |

### E. 响应式设计测试 (4个设备类型)

| 设备 | 屏幕宽度 | 预期效果 | 实际状态 |
|------|--------|--------|--------|
| **PC** | 1024px+ | 360px固定宽度，居中显示 | ✅ |
| **平板** | 768-1024px | 计算器宽90%，max 500px | ✅ |
| **手机** | 480-768px | 宽95%，按钮自适应缩小 | ✅ |
| **超小屏** | <360px | 宽98%，字体进一步缩小 | ✅ |

**测试方法**: F12 → 设备模拟 → 调整宽度查看样式响应

---

## 🚀 迭代计划 (Future Roadmap)

### v2.1 (下一个版本) - 交互增强

**新功能:**
- ✨ **键盘支持**: 
  - 数字 0-9 和 . 直接输入
  - Enter 键 = 计算结果
  - Backspace = DEL
  - Escape = 清除 (C)
  
- ✨ **表达式预览**:
  - 在显示屏上方显示当前完整表达式
  - 实时显示计算过程

**改进:**
- 优化计算器在超大屏幕 (2K/4K) 上的展示
- 增加按钮的触觉反馈 (移动端)

**预计完成**: Q2 2026

---

### v2.2 (中期版本) - 功能拓展

**新功能:**
- 📊 **历史记录** (localStorage):
  - 保存最近10次计算
  - 点击历史项可快速重新计算
  - 清除历史记录按钮

- 🎨 **主题切换**:
  - 深色模式 (Dark Mode)
  - 高对比度模式 (Accessibility)

- 📐 **更多科学函数**:
  - 三角函数: sin(x), cos(x), tan(x)
  - 对数: log(x), ln(x)
  - 常数: π (Pi), e (Euler's number)

**预计完成**: Q3 2026

---

### v3.0 (长期愿景) - 高级特性

**新功能:**
- 🧮 **矩阵计算** (可选):
  - 支持基础矩阵运算
  - 行列式、转置等

- 📱 **移动应用化**:
  - PWA (Progressive Web App) 支持
  - 离线使用能力
  - 安装到主屏幕

- 🌐 **国际化 (i18n)**:
  - 多语言支持 (EN, ZH, FR, ES...)
  - 本地化数字格式

**预计完成**: Q4 2026 或更晚

---

### 🐛 已知限制

| 限制 | 原因 | 未来方案 |
|------|------|--------|
| 不支持复数运算 | 基础计算器场景 | v3.0+ 考虑 |
| 精度问题 (浮点数) | JavaScript 原生限制 | 使用 BigDecimal 库 |
| `eval()` 安全性 | 当前本地应用，无安全隐患 | 生产环境需替换为自定义解析器 |
| 不支持括号 | 优先级处理复杂，v2.0 focus on basics | v2.2 规划添加 |

---

## 📚 附录：工程思维体系


### 按键功能速查表

| 按键 | 功能 | 示例输入 | 期望输出 |
|------|------|--------|---------|
| `0-9` | 数字输入 | `5` | `5` |
| `.` | 小数点 | `3.14` | `3.14` |
| `+` `-` `*` `/` | 运算符 | `5 + 3` | `8` (按=) |
| `=` | 计算结果 | `5 + 3 =` | `8` |
| `C` | 清除 | (任何状态) | 清空屏幕 |
| `DEL` | 退格 | `123 DEL` | `12` |
| `√` | 平方根 | `16 √` | `4` |
| `x!` | 阶乘 | `5 x!` | `120` |
| `x²` | 平方 | `3 x²` | `9` |
| `x³` | 立方 | `2 x³` | `8` |

### 异常情况处理速查表

| 异常情况 | 用户输入 | 显示信息 |
|---------|--------|---------|
| 除以零 | `5 / 0 =` | `不能除以零` |
| 负数根号 | `-4 √` | `非法输入` |
| 非整数阶乘 | `3.5 x!` | `非整数或负数` |
| 负数阶乘 | `-5 x!` | `非整数或负数` |
| 大数溢出 | `171 x!` | `数值溢出` |
| 语法错误 | `5 + =` | `语法错误` |
| 超大结果 | `(10^16) + 1` | 科学计数法 |

---

## 📚 附录：工程思维体系

本项目遵循的完整工程思维流程：

```
用户需求 (Who/What)
    ↓
功能设计 (Functional Requirements)
    ↓
交互设计 (UI/UX Requirements)
    ↓
逻辑处理 (Business Logic & Error Handling)
    ↓
非功能需求 (Performance/Compatibility)
    ↓
代码实现 (Code Quality & Maintainability)
    ↓
部署运维 (Deployment & CI/CD)
```

这份PRD不仅是功能列表，更是工程决策的记录。每一项选择（如为什么选择 Glassmorphism、为什么限制根号只支持非负数）都有其设计理由。

---

**许可证**: MIT  
**官方仓库**: https://github.com/Gusty666/web-calculator
